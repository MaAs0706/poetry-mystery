import PageContent from './PageContent';
export default function Book({phase,pageIndex,pages,introText,turning,onInteract,authorMode,draft,setDraft,seal}){const closed=phase==='CLOSED';const current=phase==='BLANK'?null:pages[pageIndex];const previous=pageIndex>0?pages[pageIndex-1]:null;return <div className={`book-stage ${closed?'is-closed':'is-open'} ${phase==='OPENING'?'opening':''}`}><button className="closed-book" onClick={onInteract} aria-label="Open the book"><span>✦</span><em>Things Never Said</em><small>open the book</small></button><div className="open-book"><div className="page-stack left-stack"/><div className="page-stack right-stack"/><section className="page left-page"><PageContent page={previous} introText={introText}/><span className="page-number">{pageIndex||''}</span></section><button className={`page right-page ${turning?'turning':''}`} onClick={onInteract} aria-label={phase==='BLANK'?'Reveal the writing':'Turn the page'}><PageContent page={authorMode?{id:'author'}:current} introText={introText} author="Her Name" editable={authorMode} draft={draft} onDraft={setDraft}/>{!authorMode&&phase==='INTRO_COMPLETE'&&<small className="turn-hint">turn the page</small>}<span className="page-number">{phase==='BLANK'?'1':pageIndex+1}</span></button><div className="spine"/>{authorMode&&<button className="seal" onClick={seal}>seal this page</button>}</div></div>}
